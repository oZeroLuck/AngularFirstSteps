import { MyButtonConfig } from './button-config';

export const ReservationsBtn: MyButtonConfig = {
  customCssClass: 'btn btn-primary',
  icon: 'format_list_bulleted',
  text: 'Reservations',
  action: 'reservations'
};
