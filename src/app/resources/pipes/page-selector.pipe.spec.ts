import { PageSelectorPipe } from './page-selector.pipe';

describe('PageSelectorPipe', () => {
  it('create an instance', () => {
    const pipe = new PageSelectorPipe();
    expect(pipe).toBeTruthy();
  });
});
